import React from "react";
interface ContributionTileProps {
    contributionCount: number;
    color: string;
    date: string;
}
export default function ContributionTile({ contributionCount, color, date, }: ContributionTileProps): React.JSX.Element;
export {};
